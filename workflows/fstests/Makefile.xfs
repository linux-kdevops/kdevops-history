ifeq (y,$(CONFIG_FSTESTS_XFS_SECTION_LOGDEV))
FSTESTS_ARGS += fstests_test_logdev='$(subst ",,$(CONFIG_FSTESTS_TEST_LOGDEV))'

export FSTESTS_TEST_LOGDEV_MKFS_OPTS:=$(subst ",,$(CONFIG_FSTESTS_TEST_LOGDEV_MKFS_OPTS))
FSTESTS_ARGS_SEPARATED += fstests_test_logdev_mkfs_opts='$(subst $(space),+,$(FSTESTS_TEST_LOGDEV_MKFS_OPTS))'
endif
